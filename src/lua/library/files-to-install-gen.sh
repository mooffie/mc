# MC developers: Run this script after you rename/remove/add files within this
# tree, if you want the 'make install' / 'make dist' commands to reflect these changes.
#
# Don't forget to run 'autoreconf' afterwards.

(
echo "# This file was generated by 'files-to-install-gen.sh'. Do not modify."
echo "files_to_install = \\"
git ls-tree -r --name-only HEAD | grep -v Makefile | grep -v files-to-install | sed '$q;s/$/ \\/'
) > files-to-install.mk
